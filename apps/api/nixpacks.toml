[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm i -g pnpm && pnpm install"]

[phases.build]
cmds = ["pnpm exec prisma generate", "pnpm build"]

[start]
cmd = "node dist/index.js"