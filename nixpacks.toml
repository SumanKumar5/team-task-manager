[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter api exec prisma generate", "pnpm --filter api build"]

[start]
cmd = "node apps/api/dist/index.js"